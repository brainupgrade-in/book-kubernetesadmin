apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hello
spec:
  rules:
  - http:
      paths:
      - path: /
        backend:
          service:
            name: hello
            port:
              number: 80
        pathType: Exact      

